apply plugin: "war"

bootWar {
    mainClassName = "id.tech.cakra.ecfcoresvc.EcfcoresvcApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
